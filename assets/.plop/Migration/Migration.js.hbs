module.exports.description = '{{titleCase scriptType}} content model for {{titleCase name}}';

module.exports.up = (migration) => {

  const {{camelCase name}} = migration.{{scriptType}}ContentType('{{camelCase name}}')
    .name('{{titleCase name}}')
    .displayField('internalName')
    .description('Content for {{titleCase name}}')

  {{camelCase name}}.createField('internalName')
    .name('Internal Name')
    .type('Symbol')
    .required(true)

};

module.exports.down = (migration) => {
  // Add your DOWN migration script here. See examples here:
  // https://github.com/contentful/migration-cli/tree/master/examples
};